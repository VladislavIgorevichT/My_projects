/*
Exam: my_exam01
Task: 17 - Range Best Selector

Input:
A command number and two non-negative integers: start and end.

Output:
Print the selected best number in the range.
For invalid input, print Invalid input.
*/

#include <stdio.h>

int sum_digits(int n);
int count_divisors(int n);
int reverse_number(int n);
int is_palindrome(int n);
int is_prime(int n);

int main(void) { return 0; }
